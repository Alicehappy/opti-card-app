import { Routes } from '@angular/router';
import { OptiCardComponent } from './opti-card/opti-card.component';

export const routes: Routes = [
    {
        path: 'opti-card', component: OptiCardComponent
    }
];
